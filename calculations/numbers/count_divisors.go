package numbers

// Erwartet eine Zahl n >= 1 und liefert die Anzahl der Teiler dieser Zahl zurück.
func CountDivisors(n int) int {
	return 0
}

// muss migt X%Y gemaxht werde "%" ist der operator für Modulo Operationen diese geben den rest iener division aus.
